machine:
  environment:
    CLOUDSDK_INSTALL_DIR: /home/ubuntu
     CLOUDSDK_CORE_DISABLE_PROMPTS: 1

dependencies:
  override:
    - curl https://sdk.cloud.google.com | bash

test:
  override:
    - gcloud --version
